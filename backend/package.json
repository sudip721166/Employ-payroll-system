{
  "name": "payroll-backend",
  "version": "1.0.0",
  "description": "Employee Payroll System Backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "npx kill-port 3000 && nodemon server.js",
    "start:kill": "npx kill-port 3000 && npm start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mysql2": "^3.6.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "kill-port": "^1.6.1"
  }
}